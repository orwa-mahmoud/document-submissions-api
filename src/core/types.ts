export type SubmissionStatus = "pending" | "under_review" | "approved" | "rejected";

export type ScanStatus = "none" | "queued" | "processing" | "done" | "failed";

export type JobStatus = "pending" | "processing" | "done" | "failed";
